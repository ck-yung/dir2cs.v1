opt: --relative
Print path name relative to the current path. It would be failed if the inqury directory is NOT in the current directory.

opt: --create-date
Get file-created-date-time other than file-last-written-date-time.
